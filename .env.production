# Used automatically by `npm run build` (vite build defaults to mode "production").
# This is baked into the built JS at build time — set the real value here (or as a
# build-time env var on whatever service builds the frontend) before deploying.
VITE_API_URL=https://REPLACE_WITH_YOUR_RENDER_BACKEND_URL
