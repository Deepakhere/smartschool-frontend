# Used automatically by `npm run build` (vite build defaults to mode "production").
# This is baked into the built JS at build time — set the real value here before
# deploying. See deploy/DEPLOYMENT.md for the actual EC2 + sslip.io setup.
VITE_API_URL=https://REPLACE_WITH_YOUR_EC2_SSLIP_DOMAIN
